[package]
name = "kache"
version = "0.1.0"
edition = "2018"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
k8s-openapi   = { version = "0.23", features = ["v1_30"] }

[dev-dependencies]
criterion = "0.3"

[[bench]]
name = "benchmark"
harness = false